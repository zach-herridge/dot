include current-theme.conf

font_family      JetBrains Mono
font_size        15.0

# Nerd Font icon fallback. The catppuccin tmux statusline and various tools use
# Nerd Font glyphs in the private-use area; plain "JetBrains Mono" lacks them, so
# they rendered as tofu/"_ _" boxes. Map those ranges to Symbols Nerd Font.
# Requires: brew install --cask font-symbols-only-nerd-font
symbol_map U+E000-U+E00D,U+E0A0-U+E0A3,U+E0B0-U+E0C8,U+E0CC-U+E0D4,U+E200-U+E2A9,U+E300-U+E3E3,U+E5FA-U+E6B7,U+E700-U+E7C5,U+EA60-U+EC1E,U+ED00-U+EFCE,U+F000-U+F2FF,U+F300-U+F381,U+F400-U+F533,U+F0001-U+F1AF0 Symbols Nerd Font Mono

window_padding_width 20 0 0 10
background #202020
background_blur 20

cursor_blink_interval 0

mouse_hide_wait 3.0

macos_option_as_alt yes
macos_titlebar_color background

# No tabs/splits — tmux handles all of that now
tab_bar_min_tabs 999

# Allow OSC 52 clipboard access (for tmux-yank, neovim, etc.)
clipboard_control write-clipboard write-primary read-clipboard read-primary
# Don't silently drop large OSC 52 copies (default cap is 512 KB). 0 = unlimited.
clipboard_max_size 0

# Allow remote control for any remaining kitty-scrollback usage (local only)
allow_remote_control yes
listen_on unix:/tmp/kitty

# Pass all keys through to tmux — no kitty-level interception
# Clear every keybinding that would conflict with tmux
clear_all_shortcuts yes

# Only keep essential macOS shortcuts
map cmd+c copy_to_clipboard
map cmd+v paste_from_clipboard
map cmd+q quit
map cmd+n new_os_window
map cmd+w close_os_window
map cmd+plus change_font_size all +1.0
map cmd+minus change_font_size all -1.0
map cmd+0 change_font_size all 0
map cmd+f toggle_fullscreen
